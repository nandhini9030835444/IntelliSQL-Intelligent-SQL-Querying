# intellisql-gemini
IntelliSQL is an AI-powered web application that converts natural language questions into SQL queries using Google Gemini models. It connects to a SQLite database and dynamically executes generated queries to retrieve structured data results. Built with Streamlit, it provides an interactive and user-friendly interface.
